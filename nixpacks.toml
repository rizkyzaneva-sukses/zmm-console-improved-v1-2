[phases.setup]
nixPkgs = ["nodejs_20", "npm-9_x", "openssl", "netcat-gnu"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npx prisma generate", "npm run build"]

[start]
cmd = "sh docker-entrypoint.sh"
